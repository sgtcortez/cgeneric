# Summary

# Introduction

Simple containers to hold generic information.   
Usually the containers use Flexible Array Member, this allows to easily allocate both container medata data(size, capacity) along with user data.